# Public URL of the separately hosted Payload CMS instance.
PAYLOAD_CMS_URL=https://cms.example.org

# Optional. Published events are public by default, so these are only needed
# if you change the Payload collection's read access rules.
PAYLOAD_CMS_API_KEY=
PAYLOAD_CMS_API_KEY_COLLECTION=users
